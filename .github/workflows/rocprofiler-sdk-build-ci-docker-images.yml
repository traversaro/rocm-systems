name: rocprofiler-sdk Build CI Docker Images

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'
  push:
    branches: [ develop ]
    paths:
      - '.github/workflows/rocprofiler-sdk-build-ci-docker-images.yml'
  pull_request:
    paths:
      - '.github/workflows/rocprofiler-sdk-build-ci-docker-images.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Needed to push/pull cached Docker layers + GHCR images
permissions:
  contents: read
  packages: write

jobs:
  # -----------------------------------------------------------------------------
  # Build a cached CI base image per OS using Buildx + GHA cache (Item #6)
  # -----------------------------------------------------------------------------
  build-ci-images:
    name: Build CI Images • ${{ matrix.os }} • ${{ matrix.gpu }}
    runs-on: azure-linux-scale-rocm
    strategy:
      fail-fast: false
      matrix:
        os: [ 'ubuntu-22.04', 'ubuntu-24.04', 'almalinux-8.10', 'rhel-10', 'sles-15.6' ]
        gpu: [ 'gfx94X', 'gfx110X', 'gfx120X' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          sparse-checkout: |
            projects/rocprofiler-sdk/ci_docker/

      - name: Login to Docker Hub
        uses: docker/login-action@184bdaa0721073962dff0199f1fb9940f07167d1 # v3.5.0
        with:
          registry: docker.io
          username: ${{ secrets.ROCPROFILER_AZURE_CI_USER }}
          password: ${{ secrets.ROCPROFILER_AZURE_CI_PASS }}

      - name: Build and Push Docker Images
        env:
          ROCDECODE_REF: develop
          ROCJPEG_REF: develop
        run: |
          chmod +x projects/rocprofiler-sdk/ci_docker/docker-build.sh
          projects/rocprofiler-sdk/ci_docker/docker-build.sh ${{ matrix.os }} --push --gpus ${{ matrix.gpu }}
